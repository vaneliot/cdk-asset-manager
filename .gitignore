*.js
!jest.config.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# Local environment overrides
.env

# CDK deploy outputs (regenerated per deploy, not secret, just environment-specific)
outputs.json
